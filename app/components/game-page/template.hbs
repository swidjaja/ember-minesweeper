<h2>WELCOME TO MINESWEEPER</h2>
<div class="game-page__content">
  <div class="game-page__content--left">
    <div class="game-page__content--header">
      <div class="game-page__timer">{{elapsedTime}}</div>
      <button aria-label="Restart Game" title="Restart Game"
        class="focusable game-page__reset-btn {{restartButtonClass}}" {{action 'restartGame'}}>
      </button>
    </div>
    <div class="game-page__content--body">
      {{grid-cells
        gameStatus=gameStatus
        gridCells=gridCells
        gridCellClicked='gridCellClicked'}}
    </div>
  </div>

  <div class="game-page__content--right">
    <section class="game-page__instruction game-page__instruction--mouse">
      <h2>Mouse users</h2>
      <p>Left click - reveal the cell</p>
      <p>Right click - flag the cell</p>
    </section>

    <section class="game-page__instruction game-page__instruction--keyboard">
      <h2>Keyboard users</h2>
      <p>Enter - reveal the cell</p>
      <p>Alt + Enter - flag the cell</p>
      <p>Alt + s - restart the game</p>
    </section>

    <section class="game-page__instruction game-page__instruction--legends">
      <h2>Legends</h2>
      <p>Number - number of cell neighbors that have mine</p>
      <p>F - cell has been flagged</p>
    </section>
  </div>
</div>

<div class="sr-only game-page__notification" aria-live="polite">{{notificationText}}</div>

